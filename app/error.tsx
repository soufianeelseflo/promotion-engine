"use client"; export default function Error({reset}:{error:Error&{digest?:string};reset:()=>void}){return <main><div className="card" style={{padding:24}}><div className="section-title">Pricing console error</div><h1>We stopped before applying anything.</h1><p className="muted">A failed admin screen should never imply a pricing mutation succeeded.</p><button className="btn btn-primary" onClick={reset}>Retry</button></div></main>}
