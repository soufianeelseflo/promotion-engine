"use client"; import {useMemo,useState} from "react"; import {Campaign} from "@/lib/types";
export function CampaignTable({rows}:{rows:Campaign[]}){const[q,setQ]=useState("");const data=useMemo(()=>rows.filter(r=>`${r.name} ${r.code}`.toLowerCase().includes(q.toLowerCase())),[rows,q]);return <div className="card" style={{padding:18}}><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search campaigns or codes…" style={{marginBottom:12}}/><div className="table-wrap"><table className="table"><thead><tr><th>Campaign</th><th>Offer</th><th>Status</th><th>Priority</th><th>Redemptions</th><th>Revenue</th></tr></thead><tbody>{data.map(c=><tr key={c.id}><td><strong>{c.name}</strong><div className="muted">{c.code}</div></td><td>{c.type==="percent"?`${c.value}%`:c.type==="fixed"?`${c.value} MAD`:"Free shipping"}</td><td><span className="chip">{c.status}</span></td><td>{c.priority}</td><td>{c.redemptions}</td><td>{c.revenue.toLocaleString()} MAD</td></tr>)}</tbody></table></div></div>}
