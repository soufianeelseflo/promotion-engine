"use client";
import {useMemo,useState} from "react"; import {campaigns} from "@/lib/data"; import {simulate} from "@/lib/engine";
export function CampaignBuilder(){const[name,setName]=useState("Weekend conversion push");const[code,setCode]=useState("WEEKEND12");const[value,setValue]=useState(12);const[minSpend,setMinSpend]=useState(600);const preview=useMemo(()=>simulate([{sku:"DEMO",name:"Sample basket",qty:1,unitPrice:1200,category:"all"}],[{...campaigns[0],id:"draft",name,code,type:"percent",value,minSpend,status:"active",stackable:false,priority:85}]),[name,code,value,minSpend]);return <div className="grid-auto"><section className="card" style={{padding:20}}><div className="section-title">Draft campaign</div><label>Name<input className="input" value={name} onChange={e=>setName(e.target.value)}/></label><label style={{display:"block",marginTop:12}}>Code<input className="input" value={code} onChange={e=>setCode(e.target.value.toUpperCase())}/></label><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10,marginTop:12}}><label>Discount %<input className="input" type="number" value={value} onChange={e=>setValue(Number(e.target.value))}/></label><label>Min spend<input className="input" type="number" value={minSpend} onChange={e=>setMinSpend(Number(e.target.value))}/></label></div><button className="btn btn-primary" style={{marginTop:14}}>Save draft</button></section><section className="card" style={{padding:20}}><div className="section-title">Live basket preview</div><p className="muted">1200 MAD sample basket</p><h2>{preview.total.toLocaleString()} MAD</h2><p>Discount: <strong>-{preview.discount.toLocaleString()} MAD</strong></p><p>Shipping: <strong>{preview.shipping.toLocaleString()} MAD</strong></p><p className="muted">Applied: {preview.applied.join(', ')||'none'}</p>{preview.warnings.map(w=><p className="muted" key={w}>• {w}</p>)}</section></div>}
